[project]
name = "lerobot_policy_diffusionqasim"
version = "0.1.0"
dependencies = [
    "lerobot",
]
requires-python = ">= 3.12"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]